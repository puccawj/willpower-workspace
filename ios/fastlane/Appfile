app_identifier("org.willpowerinstitute.app")

# TODO: fill in once you have these from https://developer.apple.com/account
# (Membership Details page has the Team ID; App Store Connect > Users and Access has Team ID too).
apple_id(ENV["APPLE_ID"] || "")
itc_team_id(ENV["APP_STORE_CONNECT_TEAM_ID"] || "")
team_id(ENV["APPLE_DEVELOPER_TEAM_ID"] || "")
